import 'package:windows_mouse_server/utils/pair.dart';

class Constants {
  Constants._();
  static const double windowsWidgetWidth = 200;
  static const double spaceBetweenWidgets = 10;
  static const Pair<int, int> defaultScreenSize = Pair(1920, 1080);
  static const Duration animatedIconDuration = Duration(milliseconds: 1000);
}
